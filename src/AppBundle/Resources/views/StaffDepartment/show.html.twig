{% extends '::base.html.twig' %}

{% block body -%}
    <h1>{{ entity.department }}</h1>
    <div class="row">
      <div class="col-xs-12">
        <p><a class="btn btn-sm btn-primary" href="{{ path('admin_staffdepartment') }}"><span class="glyphicon glyphicon-arrow-left"></span> Back to List</a></p>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-12">
        <div class="btn-group btn-group-vertical" role="group" aria-label="actions">
          <div class="btn-group" role="group">
            <a class="btn btn-sm btn-default" href="{{ path('admin_staffdepartment_edit', { 'id': entity.id }) }}">Edit</a>
          </div>
          <div class="btn-group" role="group">
            {{ form(delete_form) }}
          </div>
        </div>
      </div>
    </div>
    
{% endblock %}
