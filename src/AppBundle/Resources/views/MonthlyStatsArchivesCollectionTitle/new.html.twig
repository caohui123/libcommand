{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MonthlyStatsArchivesCollectionTitle creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('monthly_archives_components_collectiontitle') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
