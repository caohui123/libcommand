{% extends '::base.html.twig' %}
{% block title %}Library Hours: Special Events{% endblock %}
{% block body -%}
    
    {# Page Heading #}
    {% set headingvars = {'headingcontent' : 'New Special Event' } %}
    {% include 'snippets/pageHeading.html.twig' with headingvars %}
    
    {# Back button #}
    {% set vars = {'basepath' : path('hoursevent')} %}
    {% include 'snippets/newToolbar.html.twig' with vars %}
    
    <blockquote>
        <p>Special events must have a starting and ending date. Should you decide later to amend the starting and ending date of a special event, please be aware that any special hours set outside of the new date range will be <mark>deleted</mark>. The same goes for any events that are deleted outright.</p>
    </blockquote>
    <div class="row">
      <div class="col-xs-12">
          <div class="well well-lg">{{ form(form) }}</div>
      </div>
    </div>
{% endblock %}
