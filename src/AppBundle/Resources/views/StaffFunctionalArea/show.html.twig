{% extends '::base.html.twig' %}

{% block body -%}
    <h1>StaffFunctionalArea</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Functionalarea</th>
                <td>{{ entity.functionalArea }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_staffarea') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_staffarea_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
