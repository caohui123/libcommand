{% extends '::base.html.twig' %}
{% block title %}AV Request Status{% endblock %}
{% block body %}
  
    {# Page Heading #}
    {% set headingvars = {'headingcontent' : 'Status: ' ~ entity.name } %}
    {% include 'snippets/pageHeading.html.twig' with headingvars %}
    
    {# Back button, edit and print buttons #}
    {% set toolbarvars = {'basepath' : path('avrequeststatus'), 'editpath' : path('avrequeststatus_edit', { 'id': entity.id }), 'printpath' : path('avrequeststatus_print', { 'id': entity.id }), 'editrole' : 'ROLE_AV_EDIT'} %}
    {% include 'snippets/showToolbar.html.twig' with toolbarvars %}
    
    <div class="row">
      <div class="col-xs-12 col-sm-12 col-md-6">
        <table class="table table-responsive table-striped">
            <tbody>
                <tr><th>Status Name</th><td>{{entity.name}}</td></tr>
            </tbody>
        </table>
      </div>
    </div>
{% endblock %}
