{% extends '::base.html.twig' %}
{% block title %}Faculty Room Request Equipment{% endblock %}
{% block body -%}
    
    {# Page Heading #}
    {% set headingvars = {'headingcontent' : entity.name } %}
    {% include 'snippets/pageHeading.html.twig' with headingvars %}
    
    {# Back button, edit and print buttons #}
    {% set toolbarvars = {'basepath' : path('roomrequestequipment'), 'editpath' : path('roomrequestequipment_edit', { 'id': entity.id }), 'printpath' : path('roomrequestequipment_print', { 'id': entity.id }), 'editrole' : 'ROLE_ROOMREQUEST_EDIT'} %}
    {% include 'snippets/showToolbar.html.twig' with toolbarvars %}
    
    <div class="row">
      <div class="col-xs-12 col-sm-12 col-md-6">
        <table class="table table-responsive table-striped">
            <tbody>
                <tr><th>Equipment</th><td>{{entity.name}}</td></tr>
            </tbody>
        </table>
      </div>
    </div>
{% endblock %}
