{% extends '::base.html.twig' %}
{% block title %}Patron Feedback{% endblock %}
{% block body -%}
    
    {# Page Heading #}
    {% set headingvars = {'headingcontent' : 'New Feedback' } %}
    {% include 'snippets/pageHeading.html.twig' with headingvars %}
    
    {# Back button #}
    {% set vars = {'basepath' : path('feedback')} %}
    {% include 'snippets/newToolbar.html.twig' with vars %}

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('feedback') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
