{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LiaisonSubject edit</h1>

    {{ form_widget(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('liaisonsubject') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
