{% extends '::base.html.twig' %}
{% block title %}Material Reserve Media{% endblock %}
{% block body -%}  
    
    {# Page Heading #}
    {% set headingvars = {'headingcontent' : 'Media: ' ~ entity.name } %}
    {% include 'snippets/pageHeading.html.twig' with headingvars %}
    
    {# Back button, edit and print buttons #}
    {% set toolbarvars = {'basepath' : path('materialreservemedia'), 'editpath' : path('materialreservemedia_edit', { 'id': entity.id }), 'printpath' : path('materialreservemedia_print', { 'id': entity.id }), 'editrole' : 'ROLE_MATERIALRESERVE_EDIT'} %}
    {% include 'snippets/showToolbar.html.twig' with toolbarvars %}
    
    <div class="row">
      <div class="col-xs-12 col-sm-12 col-md-6">
        <table class="table table-responsive table-striped">
            <tbody>
                <tr><th>Media</th><td>{{entity.name}}</td></tr>
            </tbody>
        </table>
      </div>
    </div>
{% endblock %}
